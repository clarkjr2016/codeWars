function boolToWord(bool) {
  let returnVal;

  if (bool === true) {
    returnVal = "Yes";
  } else if (bool === false) {
    returnVal = "No";
  }

  return returnVal;
}
// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

//I start with a boolean value being entered into the function

//I return a "Yes" value if boolean equals true and a "No" value if value is false
